USE project2;
CREATE TABLE users (id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY NOT NULL, username VARCHAR(32) NOT NULL, password VARCHAR(32) NOT NULL, passwordhash BLOB(16) NOT NULL);
ALTER TABLE users ADD UNIQUE INDEX (username);
CREATE TABLE history (id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY NOT NULL, user_id INT UNSIGNED NOT NULL, query VARCHAR(2048) NOT NULL);
ALTER TABLE history ADD INDEX (user_id);
